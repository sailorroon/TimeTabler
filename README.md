This script intakes a ToDo.txt file and outputs a Timetable that includes all task within it using the tabulate and datetime library
It also generates the priority of which task should be persued first for the user, modelling the days_due factor as exponential, the priority score as quadratic.
